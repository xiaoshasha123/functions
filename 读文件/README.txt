说明：

1.材料---readfile.py(实现方法)，antiword(windows下使用)，antiword-0.37.tar.gz(linux下使用)，

         requirements.txt(导入模块)，测试文件。

2.测试---windows：将readfile.py和antiword(windows)文件夹放在同一目录下,安装所需的模块后可测试。

         linux：需编译安装antiword-0.37.tar.gz，注意路径和权限，修改readfile.py中antiword的路径。